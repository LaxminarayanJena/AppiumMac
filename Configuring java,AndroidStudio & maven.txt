Configuring environment variable for java
------------------------------------------------------------
Open terminal
1)touch.bash-profile
2)open .bash-profile
3)export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0.jdk/Contents/Home
4)export PATH=$PATH:$JAVA_HOME/bin
5)close bash profile
6)open bashprofile
echo $ JAVA-HOME

Android Studio
--------------------------
1)install nodejs through brew or directly
2)install home brew in terminal
  /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
3)brew install node
4)npm install -g appium-doctor
 if u get permission issue then only type
sudo  npm install -g appium-doctor
5)appium-doctor
6)go to appstore and download xcode
7)download android studio.dmg and install in applications
while installing copy the sdk folder path location
8)install android sdk package from sdk manager
9)create system image using avd manager

10)in terminal
 open .bash-profile
export ANDROID_HOME=/Users/jena/Library/Android/sdk
export PATH=$PATH:$JAVA_HOME/bin:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
save it

11)Download appium gui desktop and install
To install appium through terminal
npm install -g appium

https://github.com/appium/appium-xcuitest-driver
https://github.com/appium/appium-xcuitest-driver/blob/master/docs/real-device-config.md


12)brew install carthage
13)brew install libimobiledevice --HEAD
14)brew install ideviceinstaller //only works for ios 9.
  npm install -g ios-deploy//for ios 10
15)brew install ios-webkit-debug-proxy
16)gem install xcpretty

16)ios_webkit_debug_proxy -c 571hdhd677gshsh:27753
use this to conenct while working with real device

webdriver agent settings
-------------------------------------

17) cd/usr/local/lib/node_modules/appium/node_modules/appium-xcuitest-driver/WebDriverAgent/
18)mkdir -p Resources/WebDriverAgent.bundle
19)./Scripts/bootstrap.sh -d
20)open .
now open WebDriverAgent.xcodeproj
Add Your appleid and password
and click on build now it should give "build suceeded"

To work on real device

 xcodebuild -project WebDriverAgent.xcodeproj -scheme WebDriverAgentRunner -destination 'id=asssns8hdhdndhdnhd' test

maven path settings
-----------------------------------------
1)open .bash-profile
2)export MAVEN_HOME=/Users/jena/library/apache-maven-3.3
export PATH=$PATH:$JAVA_HOME/bin:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:$MAVEN_HOME/bin
save and exit





